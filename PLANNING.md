# Port Map — 기획서

> 로컬 포트 점유 현황을 실시간으로 보여주는 데스크톱 위젯

---

## 1. 제품 개요

| 항목 | 내용 |
|------|------|
| **제품명** | Port Map |
| **한 줄 설명** | 로컬 포트 점유 현황을 실시간으로 보여주는 데스크톱 위젯 |
| **형태** | 바탕화면에 항상 떠 있는 반투명 미니 패널 (프레임리스 창) |
| **타겟 사용자** | 개발자, 서버 관리자, 포트 충돌 자주 겪는 사람 |
| **배포 방식** | 오픈소스 (MIT 라이선스, GitHub) |
| **지원 OS** | macOS, Windows, Linux |

---

## 2. 주요 기능 상세 설명

### MVP (v1.0)

#### F1. 포트:프로세스 표시 (카드 그리드)

- **목적**: 현재 LISTEN 중인 로컬 포트와 점유 프로세스를 한눈에 본다.
- **입력**: 백엔드 스캐너가 수집한 포트 스냅샷.
- **동작**: `포트번호 : 프로세스명` 형태 카드를 그리드로 배치. 기본 정렬은 포트번호 오름차순.
- **출력**: 예시 카드 — `10100 : opencodex`. 카드에는 프로토콜(tcp/udp) 배지와 권한 상태 배지만 표시.
- **표시 단위**: **포트 1개 = 카드 1개** (포트 중심). 같은 PID가 여러 포트를 점유해도 포트마다 카드가 나옴 — "8080 누가 쓰나?"라는 핵심 질문에 직결.
- **엣지**: 프로세스명을 못 얻으면 `(unknown)`, 권한 부족 시 `(권한 부족)`.

#### F2. 실시간 갱신 (3초 폴링)

- **목적**: 포트 변화를 사용자가 새로고침 없이 본다.
- **입력**: 프론트엔드 `setInterval(3000)` → `fetch('/api/ports')`.
- **동작**: 백엔드는 요청 올 때마다 스캔하여 최신 스냅샷 반환. 프론트는 이전 스냅샷과 diff하여 **변경된 카드만 부분 갱신**. 전체 리렌더 금지.
- **출력**: 새 포트는 페이드인, 사라진 포트는 페이드아웃, 유지 포트는 무변경.
- **엣지**: 스캔 지연이 폴링 주기보다 길면 다음 폴링을 건너뛰고 완료 후 재시작. 연속 실패 3회 시 경고 배지.

#### F3. 검색 (즉시 필터)

- **목적**: 포트가 많을 때 특정 포트/프로세스를 빠르게 찾는다.
- **입력**: 검색창 텍스트 (포트번호 숫자 또는 프로세스명 부분문자열).
- **동작**: 입력 즉시 클라이언트 사이드 필터. 백엔드 요청 없음. 대소소문자 무감매칭.
- **출력**: 매칭된 카드만 표시, 나머지는 `display:none`.
- **엣지**: 검색 중 갱신 스냅샷이 오면 필터 조건 유지. 빈 결과 시 "검색 결과 없음" 표시.

#### F4. 항상 위 고정 (always-on-top)

- **목적**: 다른 창 작업 중에도 포트 현황이 보인다.
- **동작**: pywebview `window.on_top = True`. 트레이 메뉴에서 토글 가능.
- **엣지**: 전체화면 앱(유튜브, 게임 등) 위에 뜨면 방해됨 → v2 자동 숨김으로 보완, MVP는 수동 트레이 숨김.

#### F5. 프레임리스 반투명 + 드래그 이동

- **목적**: 위젯 느낌으로 바탕화면에 자연스럽게 녹아든다.
- **동작**: `frameless=True`, 배경 `rgba(0,0,0,0.85)`. 제목표시줄 영역(상단 28px)을 드래그 핸들로 사용 → 창 이동.
- **엣지**: 드래그 중 폴링 갱신이 오면 드래그가 끊기지 않도록 갱신 지연. 최소 사이즈 240×320px 하한.

#### F6. 더블클릭 확장 (상세 정보)

- **목적**: 필요할 때 PID, 전체 명령어, 원격 연결 정보를 본다.
- **입력**: 카드 더블클릭.
- **동작**: 카드 아래에 상세 패널 슬라이드 다운. PID, 전체 cmdline, 원격 주소(ESTABLISHED인 경우), 프로토콜/family, 권한 상태 표시.
- **출력**: 확장된 카드는 다음 폴링에서도 확장 상태 유지. 같은 카드 재더블클릭 시 접기.
- **엣지**: cmdline이 권한 부족으로 못 얻으면 `(권한 부족 — sudo 필요)` 표시. PID가 폴링 사이에 종료된 경우 "프로세스 종료됨" 표시 후 다음 갱신에서 제거.

#### F7. 시스템 트레이 (숨기기/보이기)

- **목적**: 위젯이 방해될 때 숨기고 트레이 아이콘으로만 존재. **위젯 생존성 필수** — 이게 없으면 사용자가 창을 닫고 앱이 백그라운드에서 사라진 줄 착각함.
- **동작**: 메뉴바(macOS)/트레이(Windows/Linux) 아이콘. 메뉴: 보이기/숨기기, 항상 위 토글, 종료.
- **엣지**: 창을 닫기(X)하면 트레이로 숨김(종료 아님). 트레이 메뉴 "종료"만 앱을 완전히 끝냄.

### v2.0 (이후)

#### F8. 포트 클릭 → kill

- **목적**: 충돌 포트의 프로세스를 즉시 종료.
- **보안**: PID는 정수 검증 후 `os.kill(pid, signal.SIGTERM)` 직접 호출. **절대 shell=True로 문자열 조립 금지** — 명령어 주입 위험. PID가 숫자가 아니면 거부.
- **권한**: 타 사용자 프로세스는 권한 부족 → 안내 메시지("sudo 또는 관리자 권한 필요"). 앱 자체는 sudo 요구하지 않음.

#### F9. 포트 충돌 알림

- **목적**: 감시 포트가 새로 점유되면 알림.
- **동작**: 사용자가 감시할 포트 지정. 해당 포트가 새 PID로 점유되면 OS 알림 + 카드 하이라이트.

#### F10. 다크/라이트 테마

- **동작**: 시스템 appearance 따라가기. `prefers-color-scheme` 미디어쿼리.

#### F11. 위치 기억

- **동작**: 창 위치·크기를 `~/.config/port-map/config.json`에 저장. 재실행 시 복원.

---

## 3. 기술 스택

```
┌─────────────────────────────────┐
│         pywebview (MIT)         │  ← 네이티브 창 래퍼
│  frameless + on_top + drag      │
├─────────────────────────────────┤
│    HTML/CSS/JS (Vanilla)        │  ← 프론트엔드 (신규 작성)
├─────────────────────────────────┤
│  Python stdlib http.server       │  ← 백엔드 (의존성 0)
│  psutil (권장) / lsof·netstat   │  ← 포트 스캔 (크로스플랫폼)
│  + tasklist (Windows 폴백)       │  ← 프로세스명 보강
└─────────────────────────────────┘
```

| 구성요소 | 기술 | 라이선스 | 비고 |
|----------|------|---------|------|
| 백엔드 | Python stdlib `http.server` | PSF (MIT 호환) | 외부 의존성 0 |
| 포트 스캔(권장) | `psutil` `net_connections()` | BSD-3 | 크로스플랫폼 1 API, 프로세스명 동시 획득 |
| 포트 스캔(폴백) | `lsof` (mac/Linux), `netstat` (Win) | OS 내장 | psutil 미설치 시 폴백 |
| 프로세스명 보강 | `tasklist` (Win 전용) | OS 내장 | netstat 폴백 시만 사용 |
| 프론트엔드 | HTML/CSS/JS (Vanilla, 신규 작성) | MIT | 프레임워크 없음 |
| 네이티브 창 | `pywebview` | **MIT** | 핵심 의존성 1개 |
| 패키징 | `pyinstaller` 또는 `nuitka` | GPL/Apache | 배포용 바이너리 생성 |

> 참고: 초기 기획의 "기존 코드 90% 재사용" 전제는 실제 원본 server.py / index.html이 현 저장소에 존재하지 않아 철회. server.py, index.html 모두 신규 작성한다.
>
> 참고 레포: `~/hermes-webui`의 `server.py`(ThreadingHTTPServer + SIGPIPE 처리), `api/system_health.py`(psutil 패턴), `start.sh`(`lsof -iTCP:${port} -sTCP:LISTEN`)를 구현 참고용으로 활용. 코드는 복사하지 않고 패턴만 차용한다.

---

## 4. 아키텍처 & 데이터 흐름

```
┌──────────────┐     HTTP (127.0.0.1:ephemeral)     ┌──────────────┐
│  index.html  │ ──── fetch('/api/ports') ────────→ │   server.py  │
│  (pywebview) │ ←──── JSON 스냅샷 ──────────────── │ ThreadingHTTP│
│  setInterval │                                    └──────┬───────┘
│   (3000ms)   │                                           │
│  diff 갱신   │                                    ┌──────▼───────┐
└──────────────┘                                    │  scanners/   │
                                                    │  base.py     │ ← psutil (권장)
                                                    │  mac_linux   │ ← lsof (폴백)
                                                    │  windows.py  │ ← netstat (폴백)
                                                    └──────────────┘
```

**데이터 흐름**:

1. 프론트엔드가 3초마다 `GET /api/ports` 요청.
2. server.py가 스캐너 호출 → 포트 스냅샷 생성.
3. **자기 포트 제외**: 앱 자신의 HTTP 서버 PID를 스냅샷에서 필터링 (사용자에게 앱 자체 포트가 노이즈로 보이지 않게).
4. JSON 응답 반환.
5. 프론트엔드가 이전 스냅샷과 diff → 변경 카드만 부분 갱신.

**프로세스 구조**: 단일 프로세스. app.py가 (a) 백그라운드 스레드에서 HTTP 서버 실행, (b) 메인 스레드에서 pywebview 창 실행. 종료 시 서버 shutdown 후 창 닫기.

---

## 5. 데이터 모델 & API 명세

### 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/ports` | 포트 스냅샷 반환 |
| GET | `/api/health` | 서버 상태 (스캐너 종류, 마지막 스캔 시각) |
| GET | `/` | index.html 서빙 |

### 응답 스키마: `GET /api/ports`

```json
{
  "ports": [
    {
      "port": 10100,
      "protocol": "tcp",
      "family": "ipv4",
      "state": "LISTEN",
      "pid": 12345,
      "process": "opencodex",
      "cmdline": "opencodex --port 10100",
      "remote": null,
      "permission": "full"
    }
  ],
  "scanned_at": "2026-08-03T12:00:00Z",
  "scanner": "psutil",
  "count": 1,
  "error": null
}
```

| 필드 | 타입 | 설명 |
|------|------|------|
| `port` | int | 로컬 포트번호 |
| `protocol` | string | `tcp` / `udp` |
| `family` | string | `ipv4` / `ipv6` |
| `state` | string | `LISTEN` / `ESTABLISHED` / `TIME_WAIT` 등 |
| `pid` | int\|null | 프로세스 ID (권한 부족 시 null) |
| `process` | string | 프로세스명 (못 얻으면 `"unknown"`) |
| `cmdline` | string\|null | 전체 명령어 (상세 확장 시만, 권한 부족 시 null) |
| `remote` | string\|null | 원격 주소 (ESTABLISHED인 경우 `"1.2.3.4:443"`) |
| `permission` | string | `full` / `limited` / `denied` |
| `scanned_at` | string | ISO 8601 타임스탬프 |
| `scanner` | string | 사용된 스캐너 (`psutil` / `lsof` / `netstat`) |
| `error` | string\|null | 스캔 실패 시 에러 메시지, 성공 시 null |

> LISTEN 포트만 기본 표시. ESTABLISHED는 상세 확장 시 표시. `state=LISTEN`이 핵심 질문("누가 이 포트 쓰나?")에 직결.

---

## 6. 비기능 요구사항 (NFR)

| 항목 | 목표 | 측정 방법 |
|------|------|----------|
| 폴링 주기 | 3초 (기본), 설정 가능 1~30초 | CLI `--interval` |
| 스캔 지연 | <500ms (psutil), <2s (lsof 폴백) | 서버 로그 타임스탬프 |
| 메모리 | <100MB RSS | `psutil.Process().memory_info().rss` |
| CPU (유휴) | <1% | Activity Monitor / `psutil.cpu_percent` |
| CPU (스캔 중) | <3% (순간) | 동일 |
| 시작 시간 | <2초 (바이너리), <1초 (python 직접) | app.py 부팅 타임스탬프 |
| 최대 포트 | 1000개까지 지연 <100ms 렌더 | 가상 스크롤 + diff 갱신 |
| 번들 크기 | <30MB (PyInstaller) | 빌드 산출물 크기 |
| HTTP 바인딩 | `127.0.0.1` 전용, 외부 노출 금지 | `server.bind(('127.0.0.1', 0))` |

---

## 7. 권한·보안·위협 모델

### 7.1 macOS / Linux (lsof)

- 일반 사용자 실행 시 **자신의 프로세스** 포트만 전체 정보 표시. 다른 사용자 프로세스는 PID/명령어가 누락될 수 있음.
- 1024 미만 well-known 포트(80, 443 등)는 다른 사용자가 점유한 경우 sudo 없이 정보 제한.
- sudo 실행은 보안상 기본값으로 하지 않음. 누락 항목은 `(권한 부족)` 표시로 사용자에게 상태 공개.

### 7.2 psutil 경로 (권장)

- `psutil.net_connections(kind='inet')` + `p.name()` / `p.cmdline()`로 macOS/Windows/Linux 동일 API. OS별 파이프라인 분기가 사라짐.
- 단, 다른 사용자 프로세스의 cmdline은 여전히 권한 제한. macOS에서는 자식이 아닌 프로세스 cmdline에 `psutil.AccessDenied` 발생 → `permission: "denied"` 처리.
- 의존성이 pywebview 1개 → pywebview + psutil 2개로 증가하지만, 스캐너 코드 복잡도는 절반 이하.

### 7.3 Windows (netstat + tasklist)

- `netstat -ano`는 PID만 반환. 프로세스명은 별도 `tasklist /FI "PID eq <pid>"` 호출로 보강.
- WSL 환경에서는 lsof가 보이지만 호스트 포트와 분리되므로 WSL 탐지 시 별도 표시.
- Windows는 포트 점유 프로세스 kill 시 관리자 권한 필요 → v2 kill 기능에서 UAC 상승 안내.

### 7.4 위협 모델

| 위협 | 영향 | 대응 |
|------|------|------|
| HTTP 서버가 0.0.0.0에 바인딩 → LAN 다른 기기가 포트 목록 조회 | 정보 노출 (낮음) | **127.0.0.1 전용 바인딩**, 0.0.0.0 금지 |
| kill 기능 명령어 주입 (v2) | 악의적 명령 실행 | PID 정수 검증 + `os.kill()` 직접 호출, `shell=True` 금지 |
| 앱 자신의 포트가 목록에 나타남 | 사용자 혼란 | 서버 PID로 자기 포트 필터링, 또는 `(Port Map)` 자가 태그 |
| cmdline에 민감 정보(토큰·비밀번호) 포함 | 로컬 노출 (낮음) | 표시는 하되 복사 방지는 불필요 — 로컬 단일 사용자 도구 |
| 앱 서버 포트가 다른 앱과 충돌 | 앱 시작 실패 | **포트 0 (ephemeral)** 바인딩으로 OS가 자동 할당 |

### 7.5 배포 신뢰성 (코드 서명)

| OS | 미서명 시 동작 | 권장 조치 |
|----|---------------|----------|
| macOS | Gatekeeper가 "확인되지 않은 개발자" 차단. 우회(우클릭 열기) 가능하나 최근 macOS는 더 강경 | 공식 서명+공증(Notarization) 권장, $99/년 |
| Windows | SmartScreen 경고 후 실행 가능 | EV 인증서 또는 "추가 정보 → 실행" 안내를 README에 명시 |
| Linux | AppImage/distro 패키징에 따라 다름 | AppImage 권장, 권한 chmod 안내 |

---

## 8. 위젯 UX 디테일

| 항목 | 기본값 | 비고 |
|------|--------|------|
| 투명도 | 85% (배경) | 설정에서 60~95% 조절 (v2) |
| 최소 사이즈 | 240×320px | 카드 최소 1열 보장 |
| 최대 카드 수 | 화면 내 스크롤 | 가상 스크롤 또는 페이지네이션 검토 |
| 정렬 기준 | 포트번호 오름차순 | 토글: 포트 / 프로세스명 / 최근 점유 |
| 갱신 애니메이션 | diff 기반 부분 갱신 | 전체 리렌더 금지, 깜빡임 방지 |
| 자동 숨김 | 없음 (MVP) | v2: 전체화면 앱 감지 시 자동 숨김 |
| 포커스 동작 | 블러 시 더 투명하게 | 작업 방해 최소화 |
| 빈 상태 | "점유 중인 포트 없음" 표시 | 에러와 구분 |
| 드래그 핸들 | 상단 28px 영역 | 제목표시줄 대체 |
| 카드 확장 상태 | 폴링 갱신 시 유지 | 사용자가 펼친 카드는 접히지 않음 |
| 검색 유지 | 갱신 시 필터 조건 유지 | 새 스냅샷에도 같은 필터 적용 |

### 서버 견고함 (백엔드)

| 항목 | 처리 | 출처 |
|------|------|------|
| SIGPIPE | 클라이언트 단절 시 서버 죽음 방지 (`signal.SIG_IGN`) | hermes-webui server.py |
| 동시 요청 | `ThreadingHTTPServer` 사용, 폴링 요청 겹침 대응 | hermes-webui server.py |
| 종료 시 정리 | 서버 shutdown + 커넥션 close, 좀비 스레드 방지 | 일반적 |
| 포트 충돌 | **포트 0 (ephemeral) 바인딩**으로 충돌 원천 차단 | 본 기획 |
| 자기 포트 필터 | 서버 PID로 자기 포트 제외 | 본 기획 |

---

## 9. 에러·엣지케이스

| 케이스 | 처리 |
|--------|------|
| lsof/psutil 미설치 | 폴백 체인 시도 → 전부 실패 시 설치 안내 메시지 + 비활성 상태 |
| 점유 포트 0개 | 빈 상태 화면 (에러 아님) |
| 프로세스 갑자기 종료(유령 포트) | 다음 폴링에서 자동 제거, 1주기 지연 허용 |
| 동일 PID가 여러 포트 점유 | 포트마다 카드 1개 (프로세스명 동일 표시) |
| IPv4/IPv6 동시 리스닝 | 동일 포트는 1카드로 머지, family 배지로 구분 |
| 권한 부족으로 정보 누락 | `permission: "denied"`, `(권한 부족)` 라벨 (psutil `AccessDenied` 포함) |
| lsof/psutil 파싱 실패 / 타임아웃 | 마지막 성공 데이터 유지 + 경고 배지 |
| 포트 폭발(수백 개) | 검색 필터 기본 적용 + 상위 N개만 렌더 + 가상 스크롤 |
| 앱 자신의 서버 포트 | 스냅샷에서 필터링 제외 (자기 참조 방지) |
| 스캔 지연 > 폴링 주기 | 진행 중 스캔 완료 후 다음 폴링 재개 (중복 스캔 방지) |
| 연속 스캔 실패 3회 | 경고 배지 + "스캐너 확인 필요" 메시지 |

---

## 10. 설정 & 구성

| 항목 | 기본값 | 설정 방법 | 비고 |
|------|--------|----------|------|
| 폴링 주기 | 3초 | CLI `--interval N` / 환경변수 `PORT_MAP_INTERVAL` | 1~30초 |
| HTTP 바인딩 포트 | 0 (ephemeral) | CLI `--port N` | 0 = OS 자동 할당 (충돌 방지) |
| 창 투명도 | 0.85 | CLI `--opacity 0.8` | 0.6~0.95 |
| 창 위치 | 시스템 기본 | v2: config.json 저장 | MVP는 기본 위치 |
| 항상 위 | true | 트레이 메뉴 토글 | |
| 스캐너 | auto | CLI `--scanner psutil\|lsof\|netstat` | auto = psutil 우선, 실패 시 폴백 |

> 설정 우선순위: CLI 인자 > 환경변수 > 기본값. v2에서 `~/.config/port-map/config.json` 추가.

---

## 11. 테스트 전략

### 단위 테스트

| 대상 | 테스트 내용 |
|------|------------|
| `scanners/base.py` | mock psutil 반환값으로 포트 파싱 검증 |
| `scanners/mac_linux.py` | mock lsof 출력 문자열 파싱 검증 |
| `scanners/windows.py` | mock netstat + tasklist 출력 파싱 검증 |
| 자기 포트 필터 | 서버 PID가 스냅샷에서 제외되는지 검증 |
| JSON 스키마 | 응답이 스키마에 부합하는지 검증 (필수 필드, 타입) |

### 통합 테스트

| 대상 | 테스트 내용 |
|------|------------|
| server.py | `GET /api/ports`가 유효 JSON 반환 |
| server.py | `GET /api/health`가 스캐너 상태 반환 |
| app.py | 서버 스레드 시작 후 창 로드, 종료 시 정리 |

### 수동 테스트 (DoD 연동)

- 본인 Mac에서 `lsof -iTCP -sTCP:LISTEN` 결과와 앱 표시 비교
- 포트 점유/해제 시 실시간 반영 확인
- 검색, 드래그, 더블클릭 확장, 트레이 숨기기 전 동작 확인

---

## 12. 프로젝트 구조

```
port-map/
├── app.py              # pywebview 진입점 — 서버 백그라운드 실행 + 창 띄우기
├── server.py           # HTTP 서버 + 스캐너 호출 라우터 (신규 작성)
├── static/
│   └── index.html      # UI (신규 작성)
├── scanners/
│   ├── __init__.py     # 플랫폼 감지 후 알맞은 스캐너 선택
│   ├── base.py         # psutil 기반 크로스플랫폼 (권장)
│   ├── mac_linux.py    # lsof 폴백 (psutil 미설치 시)
│   └── windows.py      # netstat + tasklist 폴백
├── assets/
│   └── icon.png        # 앱 아이콘
├── requirements.txt    # pywebview + psutil
├── LICENSE             # MIT
├── README.md           # 설치/실행/빌드 방법 + 미서명 실행 안내
└── .github/
    └── workflows/
        └── release.yml  # GitHub Actions 자동 빌드 (mac/Win/Linux)
```

---

## 13. 비용 분석

### 개발 비용

| 항목 | 비용 |
|------|------|
| 개발 인력 | **₩0** (직접 구현) |
| 개발 도구 | **₩0** (Python, VS Code, 기존 환경) |
| 테스트 환경 | **₩0** (현재 Mac M1 Max) |

### 런타임 비용

| 항목 | 비용 |
|------|------|
| 서버/클라우드 | **₩0** — 로컬 실행, 클라우드 없음 |
| API 호출 | **₩0** — 외부 API 사용 안 함 |
| 전력 소모 | 무시 가능 (psutil 3초 폴링, CPU 점유 <1%) |

### 배포/오픈소스 비용

| 항목 | 비용 | 비고 |
|------|------|------|
| GitHub 저장소 | **₩0** | Public repo 무료 |
| GitHub Releases | **₩0** | 바이너리 첨부 무료 |
| 코드 서명 인증서 | **₩0** (생략) | macOS "확인되지 않은 개발자" 경고 뜨지만 실행 가능 |
| 홈페이지/도메인 | **₩0** | GitHub README로 충분 |
| Apple Developer Program | $99/년 | **선택사항** — 공식 서명 + App Store 배포 원할 때만 |

### 크로스 플랫폼 테스트 비용 (선택)

| 항목 | 비용 | 비고 |
|------|------|------|
| Windows 테스트 | ₩0 | Parallels Desktop 기존 보유 시 |
| Linux 테스트 | ₩0 | Docker/UML로 로컬 테스트 가능 |

### 총비용

| 시나리오 | 비용 |
|------|------|
| **MVP 개발 + GitHub 배포** | **₩0** |
| v2 + 크로스플랫폼 테스트 | **₩0** (Parallels 기존 보유 시) |
| Apple 공식 서명 포함 | **$99/년** (약 ₩13만/년) |

---

## 14. 개발 일정

| 단계 | 내용 | 예상 소요 |
|------|------|----------|
| **Phase 1** | scanners/ (psutil base + lsof/netstat 폴백) + 자기 포트 필터 | 2시간 |
| **Phase 2** | server.py (HTTP 라우터, JSON 스키마, SIGPIPE, ThreadingHTTPServer) | 1.5시간 |
| **Phase 3** | index.html UI (카드 그리드, 검색, diff 갱신, 더블클릭 확장) | 2.5시간 |
| **Phase 4** | app.py (pywebview: frameless, on_top, 반투명, 드래그) | 1시간 |
| **Phase 5** | 시스템 트레이 + 빈 상태/에러 UX | 1시간 |
| **Phase 6** | 단위/통합 테스트 + 수동 DoD 검증 | 1.5시간 |
| **Phase 7** | README + LICENSE + 오픈소스 세팅 | 30분 |
| **Phase 8** | PyInstaller 빌드 + macOS 로컬 테스트 + Gatekeeper 우회 검증 | 1.5시간 |
| **Phase 9** | GitHub Actions 크로스플랫폼 릴리스 파이프라인 | 1시간 |
| **총계** | | **약 12.5시간 (1.5~2일)** |

> 초기 기획의 "총 1.5시간"은 신규 작성 분량, API 명세, 테스트, 크로스플랫폼 빌드를 과소평가한 것. 실제 구현·명세·테스트·배포 검증까지 포함.

---

## 15. 완료 기준 (Definition of Done)

### MVP v1.0 완료 조건

- [ ] 본인 Mac에서 주요 포트 10개 이상 정확 표시 (`lsof -iTCP -sTCP:LISTEN` 결과와 일치)
- [ ] 앱 자신의 서버 포트가 목록에 나타나지 않음 (자기 필터링 동작)
- [ ] 3초 폴링 갱신 시 깜빡임 없이 diff 반영
- [ ] 포트번호/프로세스명 검색 필터 정상 동작, 갱신 시 필터 유지
- [ ] 프레임리스 반투명 창, 드래그 이동 동작
- [ ] 더블클릭 시 PID/전체 명령어/원격 연결 펼침, 갱신 시 확장 상태 유지
- [ ] 시스템 트레이 숨기기/보이기/종료 동작
- [ ] 빈 상태·권한 부족·파싱 실패·연속 실패 케이스 정상 표시
- [ ] HTTP가 127.0.0.1 전용 바인딩 (외부 노출 없음)
- [ ] CPU 점유 <1% (유휴 시), 메모리 <100MB
- [ ] 단위 테스트 scanner 파싱 3종 + JSON 스키마 통과
- [ ] PyInstaller 바이너리가 macOS에서 실행됨 (서명 미포함 허용)
- [ ] README에 설치/실행/빌드 + 미서명 실행 안내 명시

---

## 16. 오픈소스 전략

| 항목 | 내용 |
|------|------|
| 라이선스 | MIT (가장 관대함, 상업적 사용도 가능) |
| 저장소명 | `port-map` |
| README | 스크린샷 + 설치 1줄 + 실행 1줄 + 미서명 실행 안내 |
| 기여 가이드 | CONTRIBUTING.md — 작은 프로젝트라 간단히 |
| GitHub Actions | push 시 mac/Win/Linux 바이너리 자동 빌드 → Release 첨부 |
| 타겟 태그 | `Productivity`, `Developer Tools` |

---

## 결론

- **비용: ₩0** — 클라우드, API, 서버 비용 전부 없음. 로컬 도구라 실행 비용도 0
- **의존성: pywebview + psutil 2개** (MIT/BSD) — psutil 도입으로 OS별 스캐너 분기 축소, 폴백은 lsof/netstat
- **코드 베이스: 전부 신규 작성** — 초기 "재사용 90%" 전제는 원본 부재로 철회
- **크로스 플랫폼: macOS/Windows/Linux** — pywebview가 전부 지원, OS별 스캐너 분리
- **권한/보안: sudo 미사용, 127.0.0.1 전용, 미서명 배포 허용** — 자기 포트 필터링, kill 주입 방지
- **일정: 약 12.5시간 (1.5~2일)** — 신규 작성 + API 명세 + 테스트 + 크로스플랫폼 빌드 포함 현실적 일정
